version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 12
    commands:
      - curl -LO "https://storage.googleapis.com/kubernetes-release/release/$(curl -s https://storage.googleapis.com/kubernetes-release/release/stable.txt)/bin/linux/amd64/kubectl"
      - chmod +x ./kubectl
      - mv ./kubectl /usr/local/bin/kubectl
  pre_build:
    commands:
      - aws ecr get-login-password --region us-east-1 | docker login --username AWS --password-stdin 741979147734.dkr.ecr.us-east-1.amazonaws.com
  build:
    commands:
      - docker build -t mooglelabs .
      - docker tag mooglelabs:latest 741979147734.dkr.ecr.us-east-1.amazonaws.com/mooglelabs:latest
      - docker push 741979147734.dkr.ecr.us-east-1.amazonaws.com/mooglelabs:latest
  post_build:
    commands:
      - CREDENTIALS=$(aws sts assume-role --role-arn $EKS_KUBECTL_ROLE_ARN --role-session-name codebuild-kubectl --duration-seconds 900)
      - export AWS_ACCESS_KEY_ID="$(echo ${CREDENTIALS} | jq -r '.Credentials.AccessKeyId')"
      - export AWS_SECRET_ACCESS_KEY="$(echo ${CREDENTIALS} | jq -r '.Credentials.SecretAccessKey')"
      - export AWS_SESSION_TOKEN="$(echo ${CREDENTIALS} | jq -r '.Credentials.SessionToken')"
      - export AWS_EXPIRATION=$(echo ${CREDENTIALS} | jq -r '.Credentials.Expiration')
      - aws eks --region us-west-2 update-kubeconfig  --name terraform-eks-demo
      - kubectl apply -f SampleApp.yaml
      - printf '[{"name":"SampleApp","imageUri":"741979147734.dkr.ecr.us-east-1.amazonaws.com/mooglelabs"}]' 741979147734.dkr.ecr.us-east-1.amazonaws.com/mooglelabs:latest > kube-SampleApp.json
artifacts:
  files:
    - kube-SampleApp.json
